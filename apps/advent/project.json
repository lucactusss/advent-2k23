{
  "name": "advent",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/advent/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/advent",
        "main": "apps/advent/src/main.ts",
        "tsConfig": "apps/advent/tsconfig.app.json",
        "assets": [
          "apps/advent/src/assets"
        ],
        "isolatedConfig": true,
        "webpackConfig": "apps/advent/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "advent:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "advent:build:development"
        },
        "production": {
          "buildTarget": "advent:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "apps/advent/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/advent/jest.config.ts"
      }
    }
  },
  "tags": []
}
